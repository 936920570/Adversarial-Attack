# Core scientific / audio
numpy>=1.21
scipy>=1.7
librosa>=0.9
soundfile>=0.12

# ML / optimization
torch>=1.13
cmaes>=0.10

# Utilities / plotting
tqdm>=4.64
matplotlib>=3.5
imageio>=2.25
requests>=2.28
click>=8.0
psutil>=5.9

# NLP / helpers
nltk>=3.8
progressbar2>=4.2  # provides `progressbar` module
Pillow>=9.0

# Optional (used by cloud_decode/tools.py helper)
timm>=0.9

# Cloud / ASR SDKs
azure-cognitiveservices-speech>=1.30
aliyun-python-sdk-core>=2.13
google-cloud-speech>=2.0.0
tencentcloud-sdk-python>=3.0.0

# Local Whisper model (used by cloud_decode/openai_function.py)
openai-whisper>=20231117

# Notes:
# - `openai-whisper` requires FFmpeg installed on your system.
# - Some scripts import modules not present in this workspace (e.g., `psychoacoustic_masking`, `cloud_decode.baidu_api`).
#   If you restore/add those files, extra dependencies may be required.
